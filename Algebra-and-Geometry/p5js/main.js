function setup(){
    createCanvas(window.innerWidth, (window.innerHeight - 53)); // The 53 refers to the height of the header. height: 53px.
    createEasyCam();
    frameRate(60);
}

function draw(){

    background(52);

}